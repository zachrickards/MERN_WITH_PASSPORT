import axios from 'axios';
import React, { useState } from 'react';
import { useHistory } from 'react-router-dom';
import { LOADING, SET_USER } from '../store/actions';
import { useStoreContext } from '../store/store';

const Inbox = () => {


  return (
    <div className="text-center">
{/* Components:
Buttons
Create new message
Update message
Messages
Profile pic
Username
Start of message body
Functionality:
Message button: Create new message
Edit/delete toggle: update current received messages
Click message component to open full chat
Click profile pic/username w/i mssg component to go to user profile

 */}
Inbox
    </div>
  );
};

export default Inbox;